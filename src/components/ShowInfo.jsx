import React from 'react'

const ShowInfo = () => {
    const {active, hanledToggle} = useActive()
  return (
    <div style={{background: "#000", color: 'white'}}
    >ShowInfo
    <div>
        <button onc> show/hide</button>
    </div>
    </div>
    
  )
}

export default ShowInfo

